spring:
  application:
    name: eatwhere-back
  datasource:
    url: jdbc:mysql://localhost:3306/eatwhere_db?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: msa505
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080