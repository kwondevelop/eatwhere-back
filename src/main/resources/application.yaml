spring:
  application:
    name: eatable-back
  datasource:
    url: jdbc:mysql://localhost:3306/eatable_db?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: eatable
    password: qwer1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080